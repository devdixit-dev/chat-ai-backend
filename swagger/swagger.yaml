openapi: 3.0.0
info:
  title: TS Express API
  version: 1.0.0

servers:
  - url: http://localhost:5000/api

paths:
  /health:
    get:
      summary: Health check
      responses:
        200:
          description: Server is healthy